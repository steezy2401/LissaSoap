import { TOrderItems } from '@/types/dropdown.types';

const orderItems: TOrderItems[] = [
  { field: 'name', title: 'Name', type: 'INC' },
  { field: 'price', title: 'Price', type: 'DESC' },
];

export { orderItems };

import { render } from '@testing-library/react';
import React from 'react';

import {{ properCase name }} from './{{ properCase name }}';

describe('{{ properCase name }}', () => {
  test('renders {{ properCase name }} component', () => {
    render(<{{ properCase name }} />);
  });
});